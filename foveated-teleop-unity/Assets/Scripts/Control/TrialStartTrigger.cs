using UnityEngine;

public class TrialStartTrigger : MonoBehaviour
{
    // Script neutered to prevent interference with live teleoperation studies.
    private void OnTriggerEnter(Collider other)
    {
        // Virtual testing triggers removed to prevent accidentally restarting live study trials.
    }
}
